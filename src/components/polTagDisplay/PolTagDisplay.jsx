import React from 'react'
import './PolTagDisplay.scss';

const PolTagDisplay = ({ label }) => {
    return (
        <p className='pol-tag-display'>{`${label}`}</p>
    )
}

export default PolTagDisplay
